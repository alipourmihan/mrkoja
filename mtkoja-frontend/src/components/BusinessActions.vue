<template>
  <div class="flex flex-wrap gap-1 sm:gap-2">
    <!-- View Button -->
    <button
      @click="$emit('view', business)"
      class="text-blue-600 hover:text-blue-900 text-sm font-medium p-1 rounded hover:bg-blue-50 transition-colors"
      title="مشاهده"
    >
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path>
      </svg>
    </button>

    <!-- Edit Button -->
    <button
      @click="$emit('edit', business)"
      class="text-yellow-600 hover:text-yellow-900 text-sm font-medium p-1 rounded hover:bg-yellow-50 transition-colors"
      title="ویرایش"
    >
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
      </svg>
    </button>

    <!-- Approve Button (only for pending) -->
    <button
      v-if="business.status === 'pending'"
      @click="$emit('approve', business.id)"
      class="text-green-600 hover:text-green-900 text-sm font-medium p-1 rounded hover:bg-green-50 transition-colors"
      title="تایید"
    >
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
      </svg>
    </button>

    <!-- Reject Button (only for pending) -->
    <button
      v-if="business.status === 'pending'"
      @click="$emit('reject', business.id)"
      class="text-red-600 hover:text-red-900 text-sm font-medium p-1 rounded hover:bg-red-50 transition-colors"
      title="رد"
    >
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
      </svg>
    </button>

    <!-- Delete Button -->
    <button
      @click="$emit('delete', business.id)"
      class="text-red-600 hover:text-red-900 text-sm font-medium p-1 rounded hover:bg-red-50 transition-colors"
      title="حذف"
    >
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
      </svg>
    </button>
  </div>
</template>

<script setup>
defineProps({
  business: {
    type: Object,
    required: true
  }
})

defineEmits(['view', 'edit', 'approve', 'reject', 'delete'])
</script>
